import { Component } from '@angular/core';

@Component({
  selector: 'app-admin-skills',
  standalone: true,
  template: `
    <section>
      <!-- TODO: CRUD de skills (POST/PUT/DELETE /api/skills) -->
    </section>
  `,
})
export class AdminSkillsComponent {}
