import { Component } from '@angular/core';

@Component({
  selector: 'app-admin-projects',
  standalone: true,
  template: `
    <section>
      <!-- TODO: CRUD de projetos (POST/PUT/DELETE /api/projects) -->
    </section>
  `,
})
export class AdminProjectsComponent {}
