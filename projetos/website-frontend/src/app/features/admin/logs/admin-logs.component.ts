import { Component } from '@angular/core';

@Component({
  selector: 'app-admin-logs',
  standalone: true,
  template: `
    <section>
      <!-- TODO: listagem + detalhe do Log Interno (GET /api/logs) -->
    </section>
  `,
})
export class AdminLogsComponent {}
